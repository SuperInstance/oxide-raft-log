[package]
repository = "https://github.com/SuperInstance/oxide-raft-log"
name = "oxide-raft-log"
version = "0.1.0"
edition = "2021"
description = "Raft-style replicated log for GPU cluster state with ternary entry status. Term conflicts, commit advancement, log compaction."
license = "MIT"
